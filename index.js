/**
 * @format
 */

import {AppRegistry} from 'react-native';
import IndoorTrainer from './IndoorTrainer';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => IndoorTrainer);
